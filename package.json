{
  "name": "vehicle-management-system",
  "private": true,
  "description": "Monorepo root — frontend lives in VMS/frontend (Vite). Vercel reads vercel.json here when Root Directory is empty.",
  "scripts": {
    "build": "cd VMS/frontend && npm ci && npm run build"
  }
}
